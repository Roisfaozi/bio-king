export function createGeometricShapes(
  canvas: HTMLCanvasElement,
  colors: any[],
  baseOpacity: number,
  speedFactor: number,
  count: number,
): any[] {
  const shapes: any[] = [];
  // ... existing code ...
  return shapes;
}

export function drawGeometricShapes(
  ctx: CanvasRenderingContext2D,
  shapes: any[],
  time: number,
) {
  // ... existing code ...
}
