export function createLiquidPoints(
  canvas: HTMLCanvasElement,
  colors: any[],
  baseOpacity: number,
  speedFactor: number,
): any[] {
  const points: any[] = [];
  // ... existing code ...
  return points;
}

export function drawLiquidGradient(
  ctx: CanvasRenderingContext2D,
  canvas: HTMLCanvasElement,
  points: any[],
  colors: any[],
  baseOpacity: number,
  time: number,
) {
  // ... existing code ...
}
